# How to Build FFmpeg on Mac

## Setup
* Ensure that the command-line tools for Xcode are installed
* If MP3 encoding is desired, install LAME using homebrew: `brew install lame`

## Build
Run `./build.sh`
